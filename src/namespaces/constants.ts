export const commandIds = {
  addConnection: 'azure-service-bus-explorer.addConnection',
  removeConnection: 'azure-service-bus-explorer.removeConnection',
} as const;

export const stateKeys = {
  connections: 'connections',
} as const;
